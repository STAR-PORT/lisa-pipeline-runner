# Sync dataset directory then reads an existing file and a missing file to test both success and failure cases of the read module.
apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  namespace: default
  generateName: test-02-read-file-
  labels:
    test: "02-read-file"
spec:
  entrypoint: pipeline
  templates:
  - name: pipeline
    dag:
      tasks:
      - name: input-sync
        templateRef:
          name: sync-module
          template: sync
          clusterScope: true
        arguments:
          parameters:
          - name: input_uri
            value: /users/admin/test-02
          - name: user
            value: admin

      - name: compute-read-success
        dependencies: [input-sync]
        templateRef:
          name: compute-read-module
          template: compute-read
          clusterScope: true
        arguments:
          parameters:
          - name: file
            value: /users/admin/test-02/existing-file.txt
          - name: cpu
            value: "2"
          - name: memory
            value: "2Gi"
          - name: user
            value: admin

      - name: compute-read-fail
        dependencies: [input-sync]
        templateRef:
          name: compute-read-module
          template: compute-read
          clusterScope: true
        arguments:
          parameters:
          - name: file
            value: /users/admin/test-02/missing-file.txt
          - name: cpu
            value: "2"
          - name: memory
            value: "2Gi"
          - name: user
            value: admin